{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Maven settings management window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
